<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"><!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoAula"
        android:icon="@mipmap/icono"
        android:roundIcon="@mipmap/icono_round"
        tools:targetApi="31">

        <!-- ===== EL CAMBIO ESTÁ AQUÍ ===== -->
        <!-- Se añade android:exported="true" para permitir que el sistema -->
        <!-- despierte al receiver cuando la app está cerrada. -->
        <receiver
            android:name=".NotificationReceiver"
            android:enabled="true"
            android:exported="true" />

        <!-- Tus Activities -->
        <activity
            android:name=".AddReminderActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity2"
            android:label="Erro Task"
            android:exported="false" />

        <!-- ÚNICA actividad de inicio -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
