// Top-level build file where you can add configuration options common to all sub-projects/modules.
// Top-level build file where you can add configuration options common to all sub-projects/modules.
// Top-level build file where you can add configuration options common to all sub-projects/modules.
// build.gradle.kts (Project: ProyectoAula)
// Archivo: build.gradle.kts (el de la raíz del proyecto)

plugins {
    // Establece la versión del plugin de Android a una moderna y estable
    id("com.android.application") version "8.2.0" apply false

    // Establece la versión del plugin de Kotlin
    id("org.jetbrains.kotlin.android") version "1.9.0" apply false

    // Establece la versión del plugin kapt
    id("org.jetbrains.kotlin.kapt") version "1.9.0" apply false
}
